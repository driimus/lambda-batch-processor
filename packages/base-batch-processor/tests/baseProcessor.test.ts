describe('BatchProcessor', () => {
  describe('#process', () => {
    it.todo('reports partial failures');
    it.todo('allows suppressing processing errors');
    it.todo('defaults to deleting messages with non-retryable errors');
  });
});
