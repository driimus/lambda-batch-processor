name: Test
on:
  push:

jobs:
  test:
    strategy:
      matrix:
        node-version: [16, 18]
    uses: driimus/shared-workflows/.github/workflows/test.yml@feat/node-version
    with:
      node-version: ${{ matrix.node-version }}
    secrets:
      turbo_token: ${{ secrets.TURBO_TOKEN }}
      turbo_team: ${{ secrets.TURBO_TEAM }}
