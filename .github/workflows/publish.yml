name: Changesets
on:
  push:
    branches:
      - main

permissions:
  id-token: write
  contents: write
  pull-requests: write

jobs:
  packages:
    uses: driimus/shared-workflows/.github/workflows/publish.yml@19-sign-commits-in-publish-workflow
    secrets: inherit
