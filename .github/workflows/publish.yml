name: Changesets
on:
  push:
    branches:
      - main

jobs:
  packages:
    uses: driimus/shared-workflows/.github/workflows/publish.yml@main
    secrets:
      NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
